import React from "react";
import ReactDOM from "react-dom";
import NewNotesApp from "./App";

ReactDOM.render(
  <React.StrictMode>
    <NewNotesApp />
  </React.StrictMode>,
  document.getElementById("root")
);
